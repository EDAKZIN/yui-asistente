# Yui AI Assistant - Dependencias Python
# Creado por EDAKZIN
# Version: 3.0.0 (Electron)

# IMPORTANTE: 
# 1. Instalar PyTorch con CUDA primero:
#    pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# 2. Para el frontend Electron, ejecutar en desktop-pet/:
#    npm install

# Core
python-dotenv>=1.0.0

# STT - Whisper con GPU (modelo "medium" recomendado)
git+https://github.com/openai/whisper.git

# LLM - Llama 3.2 3B con HuggingFace
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# TTS - Coqui XTTS v2
coqui-tts>=0.27.0

# Memoria - ChromaDB (selectiva)
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Audio Processing
sounddevice>=0.4.6
soundfile>=0.12.0
numpy>=1.22.0

# Utilities
colorlog>=6.7.0
loguru>=0.7.0

# Voice Commands - Abrir aplicaciones
AppOpener>=1.7

# Web Search - Brave Search API (usa requests)
requests>=2.28.0

# Active Listening - VAD + Wake Word
# Silero VAD se carga via torch.hub (no requiere instalación)
openwakeword>=0.5.0

# WebSocket - Comunicacion con Electron
websockets>=12.0

# Whisper dependencies
tiktoken
numba
more-itertools
