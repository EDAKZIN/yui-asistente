# Yui AI Assistant - Backend Principal
# Version: 6.2.0 (LLM GGUF Abliterated)
# Actualizado: 2026-01-16
#
# NOTA: El TTS esta aislado en tts-service/venv_tts/
# Este requirements es SOLO para el backend principal

# ==============================================================================
# INSTALACION
# ==============================================================================
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# pip install -r requirements.txt
#
# Para el TTS microservicio, ver: tts-service/requirements_tts.txt
# Para Electron: cd desktop-pet && npm install
# ==============================================================================

# Core
python-dotenv>=1.0.0
numpy>=1.24.0,<2.0

# PyTorch (CUDA 12.4 - compatible con CuDNN 9 para faster-whisper)
# Instalar manualmente: pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchaudio==2.6.0

# STT - faster-whisper (Speech-to-Text con CTranslate2)
faster-whisper>=1.0.0
ctranslate2>=4.0.0

# LLM Local - llama-cpp-python (GGUF)
# Modelo: Llama 3.2 3B Instruct Abliterated Q5_K_M
# Instalar manualmente: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# llama-cpp-python>=0.3.0  # Instalacion manual requerida para CUDA
diskcache>=5.6.0  # Dependencia de llama-cpp-python
transformers>=4.44.0
accelerate>=1.0.0  # Requerido por pysentimiento

# LLM Web - Groq API (modo rendimiento)
groq>=0.4.0

# Emociones - RoBERTuito (espanol)
pysentimiento>=0.6.0

# Memoria - ChromaDB
chromadb>=1.0.0
sentence-transformers>=2.2.0

# Audio
sounddevice>=0.4.6
soundfile>=0.12.0
PyAudio>=0.2.14

# Utilities
colorlog>=6.7.0
requests>=2.28.0

# Voice Commands
AppOpener>=1.7

# VAD + Wake Word
openwakeword>=0.5.0
openai-whisper>=20250625  # Wake word detector (OpenAI Whisper base)

# WebSocket (comunicacion con Electron y TTS Service)
websockets>=12.0
